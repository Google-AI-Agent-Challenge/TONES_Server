fastapi>=0.110.0
uvicorn>=0.28.0
pydantic[email]>=2.6.0
pydantic-settings>=2.2.0
pgvector>=0.2.0
pg8000>=1.30.0
gspread>=5.0.0
google-auth>=2.0.0
# google-cloud-sql-connector[pg8000]>=1.7.0  # cryptography 의존성으로 인한 빌드 실패를 원천 제거하기 위해 UNIX 소켓 직접 연결로 대체
# google-cloud-aiplatform>=1.48.0  # 빌드 무거움 및 컴파일 실패 유발, HTTP API 폴백 엔진으로 대체 작동
sentry-sdk[fastapi]>=1.40.0
# pytest>=8.0.0  # 테스트 전용 패키지로 빌드 경량화를 위해 주석 처리
httpx>=0.27.0
python-multipart>=0.0.9
# python-jose[cryptography]>=3.3.0  # C/Rust 컴파일러 부재로 인한 도커 빌드 실패 방지를 위해 제거
# passlib[bcrypt]>=1.7.4  # C/Rust 컴파일러 부재로 인한 도커 빌드 실패 방지를 위해 제거
# bcrypt>=4.0.1  # C/Rust 컴파일러 부재로 인한 도커 빌드 실패 방지를 위해 제거
pyjwt>=2.8.0  # 순수 파이썬(Pure Python) JWT 구현체로 대체 탑재 (컴파일 에러 0%)
# pandas>=2.0.0  # 로컬 크롤러 전용으로 도커 빌드 시 컴파일 에러 예방을 위해 주석 처리
# openpyxl>=3.1.0  # 로컬 크롤러 전용으로 도커 빌드 시 컴파일 에러 예방을 위해 주석 처리
# selenium>=4.0.0  # 로컬 크롤러 전용으로 도커 빌드 시 컴파일 에러 예방을 위해 주석 처리
# curl_cffi>=0.6.0  # 로컬 크롤러 전용으로 도커 빌드 시 컴파일 에러 예방을 위해 주석 처리
